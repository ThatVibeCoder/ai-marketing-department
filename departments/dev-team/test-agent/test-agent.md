# Test Agent

Tries to break what the department builds, before anyone else can. A page before it goes live, a key after setup, a routine before it runs on a schedule.

Nothing to connect, it only needs the repo.

## Memory
